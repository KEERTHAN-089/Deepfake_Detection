rules_version = '2';
// Browsers never read or write Firestore directly: the backend uses the Admin SDK,
// which bypasses these rules. Deny all client access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
